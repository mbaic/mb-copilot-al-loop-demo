---
description: Fix an AL build error in a self-checking loop, max 3 attempts.
---

Fix the following Business Central AL build problem using the `al-build-loop` agent's rules: build, check diagnostics, fix, repeat, and stop at 3 attempts or a clean build.

Problem: ${input}
