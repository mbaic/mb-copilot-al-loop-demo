# mb-copilot-al-loop-demo
A minimal VS Code extension that demonstrates loop engineering for Business Central AL: a Copilot custom agent that fixes a build error in a self-checking loop, with a real, code-enforced attempt limit.
